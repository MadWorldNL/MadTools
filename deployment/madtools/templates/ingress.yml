apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress
  namespace: {{ .Values.namespace }}
  annotations:
    cert-manager.io/cluster-issuer: "lets-encrypt"
    cert-manager.io/common-name: {{ .Values.madtools.api.host }}
    cert-manager.io/subject-countries: {{ .Values.certification.country }}
spec:
  {{- if .Values.clusterIssuer.enabled }}
  ingressClassName: nginx
  tls:
    - hosts:
        - {{ .Values.madtools.api.host.default }}
        - {{ .Values.madtools.api.host.ipv4 }}
        - {{ .Values.madtools.api.host.ipv6 }}
      secretName: madworld-tls
  {{ end }}
  rules:
    - host: {{ .Values.madtools.api.host.default }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ .Values.madtools.api.loadBalancer.default }}
                port:
                  number: 8080
    - host: {{ .Values.madtools.api.host.ipv4 }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ .Values.madtools.api.loadBalancer.ipv4 }}
                port:
                  number: 8080
    - host: {{ .Values.madtools.api.host.ipv6 }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ .Values.madtools.api.loadBalancer.ipv6 }}
                port:
                  number: 8080